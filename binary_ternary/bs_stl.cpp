// Primer not < (primer elemento o mayor)
lower_bound(Iterator first, Iterator last, Find x);
// Primer > (elemento siguente del último)
upper_bound(Iterator first, Iterator last, Find x);
// @end si no se encontró en ambos casos
